#include "contacts.h"
#include "operations.h"
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<sqlite3.h>
int main(int argc, char const *argv[])
{
    loop();
    // contacts contact;
    // contact = create_contact("Tingyu", "15906669899", "Zhejiang");
    // int i = 0;
    // while(contact.telnum[i] != 0)
    // {
    //     printf("%c", contact.telnum[i]);
    //     i++;
    // }
    
    // printf("%s,%s,%s\n", contact.name, contact.telnum, contact.adress);
    // int a = 2, b = 3;
    // Stu test = getStu(a, b);
    // printf("%d, %d\n", test.a, test.b);
    return 0;
}
